# Handoff 2026-07-29 (end of session)

Internal session context. Not shown on /releases (the importer excludes
handoffs). Read the release report (demo/release-report-2026-07-29.md) for the
feature-facing summary.

## What shipped this session

- **Releases changelog (Instinct)** in PR #170 (branch
  `chore/release-notes-backfill`): the /releases page (month tabs, analytics
  strip, Products view), the API + `instinct_releases` table (migration 223),
  the generator (`npm run release:notes`), and the backfill importer
  (`npm run release:backfill`). Data is already live in prod (35 items); the
  UI ships when #170 merges.
- **Security sweep** across all three repos, merged: Auto #53/#55/#56, Instinct
  #167, LMS #2. Every Dependabot alert cleared; code-scanning findings fixed;
  a reusable `security-hygiene.yml` added to each repo.
- **Forgot-password + login remediation (Auto)** folded into #52 (merged): reset
  page made public, sidebar hidden for logged-out users, reset email awaited so
  it sends, and the missing `dealer_users` MFA columns backfilled to prod
  out-of-band (that was the real cause of every login failing). PR #57 was the
  main-based version and was closed as superseded by #52.

## Open follow-ups

1. **Merge PR #170** to ship the /releases UI. All checks green; my admin/normal
   merge is blocked by the auto-mode classifier, so it needs a human merge.
2. **SDLC plan** was delivered and re-surfaced. Agreed next step before this
   detour was Sprint 1 (LMS parity: verify.sh, /api/version, Sentry; replicate
   handoff scaffold; shadow-sweep asserts 200). Resume there.
3. **Lines-of-code stat** counts git-tracked source by extension across repos,
   excluding node_modules/vendored/generated/.d.ts. Total ~1.83M. If a tighter
   definition is wanted (exclude tests, specific languages), it is one function
   in `scripts/backfill-release-notes.ts`.
4. **OGIAM** is on the timeline via its GitHub creation date only (not cloned
   locally), so it has no LOC and no reports imported. Clone it to include more.

## Account-level items (not code)

- **Neon egress** on wolfpack-auto hit the monthly 5 GB. The polling leak is
  fixed; remaining drivers are the nightly DAST crawl of prod and the absence of
  a Redis cache. Offered to retarget DAST off prod and wire `REDIS_URL`.
- **GitHub Actions billing** tipped over mid-session (a spending-limit failure
  blocked new runs). Resolve in the org Billing and plans settings.

## Standing reminders

- Commit author: `Nicholas Homyk <25436368+nhomyk@users.noreply.github.com>`.
- No Co-Authored-By trailer on commits; no em dashes anywhere.
- /releases is team-facing: features + milestones only, never internal handoffs.
