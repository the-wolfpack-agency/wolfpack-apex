# Wolfpack Instinct — Session Handoff
**Date:** 2026-07-07
**HEAD commit:** `99e89541`
**Deployed:** https://wolfpack-instinct.vercel.app
**Repo:** the-wolfpack-agency/wolfpack-apex

---

## Headline

Shipped the **Invoice Tracker** (one `/invoices` hub: the Vendor AP queue + a read-only PCNA SharePoint mirror) and fixed the **silent deployment-notification gap** — the release-gate notifier was fully built but its cron was never scheduled, so the bell never fired; a new cron-coverage guardrail then caught a second orphaned cron.

> Scope note: the "What Shipped Today" list below is auto-generated as *everything since the last handoff (2026-06-09)* — a month of backlog. **This session (2026-07-07)** was specifically the three invoice/deploy PRs (`#133`, `#134`, `#135`), the earlier feedback-inbox fixes, and — in a separate repo — the Porsche Experience OS theming (see Conversational Context).

---

## What Shipped Today
_(Commits since the last handoff on 2026-06-09.)_

### Features
- `99332902` feat(invoices): read-only PCNA invoice tracker mirroring SharePoint (#133)
- `b5c59bcd` feat(compliance): data-protection + tenant-isolation controls (close real gaps) (#131)
- `573db2a5` feat(ai-surface): self-scan dogfood + case study (npm run self-scan) (#130)
- `a72aa354` feat(auth): admin MFA (TOTP) — opt-in, self-service, NON-ENFORCING (#125)
- `e2421d0a` feat(deploy): recommended merge order on the release gate (#127)
- `10fb4404` feat(ogiam): governance drift trends + regression alerting (#124)
- `27dc001f` feat(ogiam): one-click demo reset (seed all five demo beats) (#121)
- `9087d87c` feat(compliance): forwardable, signed evidence export for the Comply beat (#123)
- `fbcf4e1d` feat(ai-surface): live public-repo scan + deterministic remediation (#122)
- `b1e1e889` feat(compliance): add Compliance Evidence UI (demo beat "Comply") (#120)
- `aa11982f` feat(ui): AI Red-Team assurance page (the Assure demo beat) (#117)
- `7d4de3af` feat(ui): AI Surface Inventory page (the discover demo beat + free-scan view) (#116)
- `4fd64e7e` feat(compliance): auto-generate SOC2 / ISO 42001 / NIST AI RMF / EU AI Act evidence from live controls (#114)
- `7927842c` feat(ai-redteam): continuous adversarial assurance of the OGIAM gate (#112)
- `f420de0b` feat(deploy): Production Release Gate - in-product deploy visibility + one-click promote (#105)
- `0217e3a2` feat(platform-scan): safe active-learning benchmark corpus + ground-truth scorer
- `61542ed7` feat(ogiam): ingest abuse-hardening + gate self-verification harness
- `4a2eb89c` feat(ogiam): bring-your-own-agent gate-as-a-service (external API-keyed gate endpoint)
- `3fe4570a` feat(platform-scan): deterministic gate-native scripted-journey runner
- `d63bb72a` feat(platform-scan): UX posture score + continuous UX sweep + tier-2 journey-friction foundation
- `53cf30a8` feat(platform-scan): axe-core accessibility findings in the tier-1 UX scan
- `a0dcc0cf` feat(platform-scan): tier-1 read-only Playwright UX capture runner
- `ce82e325` feat(platform-scan): gate-as-a-service endpoint + server-side classification of browser observations
- `32327112` feat(platform-scan): gated browser-UX layer - usability detectors + model-agnostic action gate
- `e2d9e76e` feat(platform-scan): wire wolfpack-auto manifest login to NextAuth credentials
- `30899978` feat(platform-scan): NextAuth/Auth.js credentials auth for authenticated scanning
- `3aae4310` feat(connectors): editable connector name for form-login / oauth-password connections
- `82d296ee` feat(pentest): auth-bypass + injection probes; multi-tenant scoping guardrail
- `29b9b37f` feat(onboarding): client onboarding wizard UI (over the stored target registry)
- `3eee0714` feat(onboarding): stored client scan-target registry (onboard without code change)
- `f8d7076c` feat(pentest): authorized engagement runner (in-product live run + cron + console)
- `40415b71` feat(pentest): graduate gate policy, surface findings (console + report), fix rate-limit precision
- `92ba8191` feat(pentest): rate-limit + info-disclosure probes (active suite rounded out)
- `39050a6a` feat(pentest): operator console UI for the safety harness
- `2447e7d3` feat(pentest): IDOR confirmation probe on the safety harness (first active probe)
- `7a1ba4c6` feat(pentest): active-pentest safety harness (fail-closed, no probe yet)
- `c483157e` feat(platform-scan): gated remediation PR (accept a recommendation -> a PR)
- `93d972f8` feat(platform-scan): continuous engagement orchestrator (autonomy heartbeat)
- `b67e95df` feat(platform-scan): automation recommendation engine (propose, gate-governed)
- `9d55bf06` feat(platform-scan): client System Profiler (the knowledge spine)
- `303b5b75` feat(reports): client Security Engagement report (data-backed deliverable)
- `e32760e7` feat(platform-scan): HTTP security-header/cookie/CORS checks + SARIF ingest
- `52169f2d` feat(platform-scan): provider-signature secret detection (precision-first)
- `2c5b86bc` feat(audit-ui): one-click "Reconcile all forks" on /admin/audit
- `27c6228c` feat(platform-scan): auto-resolve findings a re-scan no longer detects
- `c1a439d0` feat(platform-scan): triage UX (severity filter + bulk) + audit re-anchor button + enforced platform CI gate
- `f8d42fc8` feat: agent CRM execute fix + tabbed detail page + approval history + findings dedup
- `be1f1154` feat: backup-agent failover + demo-login canary + audit-chain concurrency fix + dropdown fix
- `6658be92` feat(security): Tier-1 production hardening (binding enforcement + alerting + audit cron)
- `c1c381b1` feat(agents): bind a connection to an agent (a "Salesforce agent", a "Jira agent")
- `f2a21074` feat(connectors): Salesforce oauth-password connector (authenticate + execute on a sandbox)
- `0a54335b` feat(agents): connect an agent to a client system + scan any connection
- `691f0b40` feat(platform-scan): authenticated connection (username/password) + gray-box API probe + agent email invite
- `368a8521` feat(platform-scan): 7 detectors, whole-repo discovery, dashboard rollup
- `d4705caf` feat(platform-scan): static + browser layers, route auto-discovery, platform selector
- `9db0581e` feat(platform-scan): agents scan a target platform for bugs + use-case gaps
- `51a68176` feat(agents): surface an agent's pending writes on its detail page (close the loop)
- `c7ab8a5d` feat(connectors): wolfpack-auto dealer preset (cross-product agent trial)
- `d6d2de53` feat(health): nightly integration-health sweep + schema-drift learning event
- `3d4136d8` feat(agents): write-approval queue UI (/admin/agents/approvals)
- `7a94eb13` feat(agents): human-in-the-loop write approvals (capture -> approve -> execute)
- `6d20d040` feat(ogiam): fail closed on unauditable in enforce mode
- `73756e9a` feat(site-analytics): ingest ogiam.com usage into our own platform + reuse the heatmap
- `ccd2e974` feat(agents): draft_email operation - the safe terminal step of the cross-tool workflow
- `c00eccf8` feat(ogiam): make the governance ledger tamper-evident at the DB layer
- `8826a0f0` feat(agents): create_document operation (fix no_match) + prominent Assign CTA
- `ef15f6bd` feat(agents): result-chaining so a step can use earlier steps' output
- `2c8a4e86` feat(agents): real web-search capability, multi-action decomposition, untruncated titles
- `1567bc00` feat(agents): true prompt-to-response action trail + Brain ingestion
- `ea40d4c7` feat(agents): Agent log on the detail page (the IAM audit trail)
- `45c85cc4` feat(dashboard): Microsoft 365 disconnected banner with one-click reconnect
- `a7ae3b71` feat(microsoft): notify the user when their Microsoft grant is revoked
- `1d18b0b0` feat(agents): declarative operation registry, agents invoke any platform function
- `5b300556` feat(agents): generic on-behalf execution, no per-workflow code
- `372703de` feat(agents): secure on-behalf delegation + shared form-action executor
- `2f2199b2` feat(agents): Brain-grounded, model-routed, maturation-measured execution
- `02328afd` feat(ai): cost-aware in-house model registry and router
- `11cda9bd` feat(agents): execute assigned work inline and show live progress
- `ac99f62b` feat(agents): human-triggered scan, profile run-scan button, nav entry point
- `9031aa8c` feat(agents): Phase E drift governance + assistant-driven agent delegation
- `4e3c44d4` feat(agents): shared memory UI
- `33859a21` feat(agents): shared memory API with a human promote/reject override
- `e7e4602a` feat(agents): cumulative memory inheritance with an anti-poisoning gate (Phase D)
- `d1f3b40e` feat(agents): assigned-work view on the agent profile
- `ed72772a` feat(agents): task endpoints (assign, list, and the agent runtime)
- `84dbc810` feat(agents): governed multi-step task executor (Phase C)
- `a67ea3d6` feat(agents): show the learned system model on the agent profile
- `4e51ee74` feat(agents): scan + act endpoints and the agent-filtered decision explorer
- `44cf630e` feat(agents): self-onboarding scan (the agent learns the platform)
- `3e6ec5c2` feat(agents): run the assistant dispatcher as the agent's own principal
- `e922059a` feat(agents): roster + profile UI and onboarding e2e
- `832d5408` feat(agents): onboarding API (invite, roster, lifecycle, agent login)
- `1e479d38` feat(agents): agent principals with a portable identity and a hard auth boundary
- `a000d65e` feat(ogiam): signing self-test endpoint + offline signature verification
- `afdf94a0` feat(ogiam): hourly checkpoint sweep + chain verification in the explorer
- `bdad5a44` feat(ogiam): sign and verify the decision chain (non-repudiation)
- `5fd9cb5b` feat(ogiam): decision explorer for the shadow ledger
- `e81ce8a1` feat(ogiam): deterministic AI action gate, Phase 0 shadow mode
- `a30b2952` feat(feedback): notify the feedback readers when new feedback arrives
- `13fca2be` feat(feedback): attach a screenshot to assistant feedback
- `2c381b0d` feat(ai-gateway): outbound LLM PII redaction, semantic cache, cost insights

### Fixes
- `99e89541` fix(deploy): schedule the release-gate notifier so deployment activity hits the bell (#135)
- `bb0b560f` fix(invoices): land the hub rework that #133 merged without (#134)
- `4a3077d2` fix(feedback): exclude E2E smoke-test noise from the inbox (unbury real feedback)
- `74a46deb` fix(feedback): reader sees EVERY note org-wide (not workspace-scoped)
- `5e2ee415` fix(feedback): inbox shows feedback from every workspace the viewer reads
- `faf8371b` fix(mail): never send to undeliverable seed domains (stop the wolfpack.dev bounces)
- `0e09b0f1` fix(tenant-isolation): repo-wide cross-tenant guardrail + close job-codes dossier leak (#111)
- `19de638f` fix(agents): agent dispatch path registers the FULL tool set (CRM read no_match) (#109)
- `f3dc3e11` fix: prod hotfixes - agent on-behalf fetch, email spam, copy; refresh market analysis (#107)
- `d51d6ff7` fix(ui): responsive bugs on console pages + posture drill-down + validation map (#106)
- `95eb24d3` fix(platform-scan): precision-tighten empty-catch detector (74 -> 2 on a real repo)
- `bccf696c` fix(db): allow nextauth_credentials in the connector auth_type CHECK (migration 199)
- `9be0f1da` fix(platform-scan): precision-first CORS severity (kill ACAO:* noise on HTML/CDN defaults)
- `4cb24042` fix(connectors): Verify form-login + oauth-password connectors by actually logging in
- `52fd9bdb` fix(connectors UX): order the form so the type selector precedes the fields it controls
- `c5bae98e` fix(deploy-readiness): honor the legacy APEX_JWT_SECRET fallback the app actually uses
- `83506dc0` Fix assistant reply vanishing seconds after it renders
- `e396d92f` Fix all pre-existing jest failures (87 -> 0) across 30 suites + clean tsc
- `541e41df` fix(audit): reconcile notification lists only the newly-anchored seqs
- `366db1d7` fix(platform-scan): recognize read-body-then-check idiom (silentFetch precision)
- `f518c73d` fix(audit): self-heal legacy concurrency forks; keep tamper break-glass
- `63b31f60` fix(platform-scan): treat headers/blob/text reads as a guard (silentFetch precision)
- `61039bed` fix(platform-scan): widen silentFetch guard window 6->12 (fewer false positives)
- `e918c770` fix(platform-scan): triage detectors for precision (629 -> 165 real findings)
- `441675a3` fix(platform-scan): validate detectors on real source + harden the prod read path
- `14b04ac2` fix(messages): stop meeting invites from firing a blank Teams notification
- `cbe30b5e` fix(brand): render OGIAM sidebar mark as a true CSS circle
- `bfc58570` fix(brand): drop white canvas on sidebar OGIAM icon; clip to circle
- `d7fca88a` fix(prebrief): only label OOO that overlaps today; swap brand to OGIAM logo
- `e16d33ab` fix(microsoft): detect a revoked Graph grant and prompt reconnect, not a blank
- `91a568f6` fix(calendar): timezone normalizer can no longer throw and blank the calendar
- `03e41026` fix(agents): live-list fallback for agent task create + show step error detail
- `5cd10751` fix(agents): agents execute create-task for real (on behalf of owner)
- `1dd9cd55` fix(agents): correct task status + stop agents invoking the delegation tool
- `31514008` fix(dashboard): scope Today's Schedule to the user's local day
- `23a6dba3` fix(ooo): show out-of-office on the correct day
- `7ba94448` fix(calendar): normalize Microsoft Graph datetimes to true UTC instants
- `52546ddf` fix(security): make capability + audit coverage guardrails green
- `0abcb516` fix(feedback): stop auto-recording natural language, collapse and clean dupes
- `d16eacfa` fix(assistant): let screenshots attach, and stop rejecting distinct photos
- `44216b24` fix(assistant): keep the floating panel collapse button on-screen
- `d4e8f7a4` fix(messages): normalize non-breaking spaces in stripHtmlToText
- `9107fdc1` fix(messages): live-update the open thread, not just the chat list
- `1b82fff6` fix(login): use raw fetch so an expired session never blocks sign-in
- `40be778a` fix(admin/feedback): collapse duplicate submissions in the inbox view
- `1e38ed54` fix(self-scan): line-independent baseline identity (stop line-shift false-reds)
- `78b69232` fix(dashboard): graceful degradation — one slow endpoint can't blank the page
- `06742b6e` fix(self-scan): scan Instinct only, not the bundled AgenticQA tool
- `cd2e81b1` fix(security): resolve 6 CodeQL code-scanning alerts

### Docs
- `f4ae320c` docs: refresh whitepaper two-pager + reassess pricing for shipped capabilities (#128)
- `68d3d927` docs(gtm): enablement v2 + public governance-sample page + deferred security track (#126)
- `cf4503ef` docs(pitch): personalized founder GTM playbook (#119)
- `3637db83` docs(pitch): flake-proof demo runbook + outreach and deck (#118)
- `9d45f5c8` docs(pitch): investor demo script + pitch kit (#113)
- `9e961a22` docs(ogiam): pentest pricing strategy, IP research, client whitepaper (#110)
- `d6e9ef98` docs: clarify Phase 1 is already done for a running deployment; note once-shown secrets
- `7beab73f` docs: add direct links (OGIAM admin pages + external consoles) to the install manual
- `da24579f` docs: expand activation guide into a complete click-level install + ops manual
- `4c622de0` docs: OGIAM activation + client deployment guide (owner go-live instructions)
- `6eb88500` docs: OGIAM market analysis (internal, grounded)
- `7fde199c` docs: OGIAM pricing/packaging + client deployment plan (internal, draft)
- `bb6a7deb` docs: 2026-06-09 handoff + release report (Instinct survey platform + Weekend with Porsche) + hosting/decision docs

### Other
- `b08192bd` chore: auto-union append-only registry files (.gitattributes merge=union) (#129)
- `79918c43` ci(reality-check): fix slow + flaky e2e (target deployed app, cache browser, split PR/push) (#115)
- `c5a87d68` ci(deps): bump the actions-ecosystem group across 1 directory with 12 updates (#108)
- `ca956955` Add three adjacent self-hosted labeled benchmark targets (#104)
- `2b3586ba` Scan-engine politeness + continuous-sweep failure alerting
- `c3dcdf02` Close four scaling/safety gaps: GitHub App, AI budget enforcement, tenant isolation, client offboarding
- `41eabe74` Close three deployment-failure gaps: target ownership, deploy readiness, scan coverage
- `cd0efde6` Onboard-to-scan integration test + dependency security cleanup
- `03b21288` Add onboarding preflight readiness check + single-source-of-truth runbook
- `219585ae` harden(pentest/onboarding): case-insensitive host allowlist + reject private-IP baseUrl at registration
- `1f781d52` security(platform-scan): SSRF guard on scan targets
- `2614d0cd` test(onboarding): full client-onboarding e2e (integration + Playwright) + invite-email UI
- `058b0099` test(agents): prove an agent reads a client's Salesforce end to end
- `dcf6b7ba` QR: support a branded redirect host (QR_PUBLIC_ORIGIN) for the scan preview
- `48ee7516` QR: add native EPS export + tie every download into the learning loop
- `4fe548d7` chore: redeploy to pick up SITE_ANALYTICS_INGEST_TOKEN
- `edc86335` test(ogiam): end-to-end audit-trail closure + ledger immutability coverage
- `f3eb117c` style(brand): align the Instinct app palette + background to ogiam.com
- `c933dea2` chore: scripts/verify-deploy.sh to confirm prod serves the current commit
- `9cc06f41` chore: public /api/version endpoint to confirm the live build SHA
- `8ca9c5ea` test(agents): e2e guard that an agent actually creates a task end to end
- `f2aadeb8` test(agents): dual proof of the governed agentic workforce
- `7b1467be` test(ogiam): replay adversarial prompts through the live dispatcher + e2e
- `a7e6d4d1` test(ogiam): e2e shadow-flow spec and CI wiring
- `17e3b481` test(e2e): cover the feedback screenshot journey end to end
- `22cbcf38` test(assistant): align file-attachments assertions with current behavior
- `47417889` perf(messages): poll the open thread every ~13s, list stays at ~60s
- `c7d8aa4d` security(self-scan): mark intentionally-public routes + refresh baseline
- `75522d07` chore(security): npm audit fix (brace-expansion >=5.0.6, ws >=8.20.1)

---

## Conversational Context (the part that matters tomorrow)

**Invoice Tracker (`#133` → `#134`, live)**
- Built off the `/job-codes` pattern (read-only live mirror of a SharePoint xlsx via Graph). Scoped to **PCNA** for now but **config-driven** (`src/lib/invoice-tracker/config.ts`) so another company is one entry + its share URL.
- **Read-only by explicit decision.** I first recommended read-only; the CTO pushed back that job-codes is editable, then confirmed "keep read-only for now." Editing is an additive layer later (job-codes' `cell-writer.ts` is the safety pattern to copy: identity-column match + column allowlist + pre-write verify).
- **Access = explicit email allowlist** (finance data, least privilege): `homyk@`, `nick@`, `jorge@thewolfpack.agency`. Not a role capability.
- **The rework mattered:** the first cut (`#133`) added a *second* "Invoices" nav item pointing at a new `/invoices`, shadowing the existing AP upload page. `#134` fixed it: one `/invoices` hub with **Vendor Invoices** (the existing `InvoicesPanel`) + **PCNA** as sub-pages, breadcrumbs on both, and `/finance/invoices` now **redirects** to `/invoices/vendor` (one mount, DRY). Nav is OR-gated (finance roles OR the PCNA viewers).
- **CROSS-TENANT GOTCHA:** the PCNA workbook lives in PCNA's tenant, so it's read with the **viewer's connected Microsoft (delegated) token**, app-only fallback. Nick's MS account must be connected in Instinct AND have SharePoint access to the file, or `/invoices/pcna` shows a "connect Microsoft" empty state. Optional env `INVOICE_TRACKER_PCNA_SHARE_URL` rotates the share link without a code change.

**Deployment notifications (`#135`, live)**
- Complaint: `/admin/deployment` shows GitHub/release activity but nothing hit the bell. **Root cause: `/api/cron/release-gate-check` was never in `vercel.json`** — the notifier code was perfect, just never invoked. Scheduled it `*/30`.
- Retuned the notify policy to a **per-state threshold map** (`STATE_THRESHOLD_HOURS`): `ready_to_merge` pings promptly (the "waiting to deploy" signal), `awaiting_approval` 4h, `checks_failing`/`merge_conflict` 8h stall (unattended red PR / dep bump = "tests failing - fix needed"), `checks_running` never. **All anti-spam guards left intact** (email OFF by default, 6h cooldown, fail-closed dedupe, per-run cap 3).
- Added `src/__tests__/cron-schedule-coverage.test.ts` (every cron route must be scheduled or allowlisted). It immediately caught **`principles-weekly-report`** also orphaned (docstring says "Mondays 8am UTC") → scheduled `0 8 * * 1`.

**Separate repo — Porsche Experience OS (`wolfpack-porsche-weekend`, weekendwithporsche.com)**
- Changed brand accent **Guards Red `#d5001c` → Porsche Black `#000000`** across `/admin` (token `--wp-accent` + 3 hardcoded rings). The **Pending** status chip is now black too — CTO may still want it red as a status color (open decision).
- Iterated the floating assistant launcher icon per feedback: removed the Porsche **crest** → tried the **wordmark** → settled on a **neutral chat glyph** (crest/wordmark read as branding, not an actionable control). Also removed the "(e.g. 'where's the heatmap?')" hint example.
- **Deploy mechanic differs from apex:** the whole admin OS lives on the unmerged branch `feat/admin-dealer-os` (PR **#16**); `main` is the marketing site. Prod domain runs the branch build via manual **`npx vercel --prod`** (project `porsche-weekend-site`). `DATABASE_URL` is scoped separately Preview vs Production — never `vercel promote` a preview to prod (wrong DB); always fresh `--prod`.

---

## Open Items / What's Next

- **Verify the deploy notifier on the live URL** (green tests aren't enough): from `/admin/deployment` trigger a manual sweep and confirm bell notifications appear. If none, read the cron JSON (`{checked, notified, degraded, dedupeUnavailable}`) to see whether it found changes or honest-degraded on a GitHub read.
- **Confirm `/invoices/pcna` renders live** against Nick's connected Microsoft account (cross-tenant read can't be proven from unit tests).
- **Notifier cadence** is 30 min — drop to 15 if faster awareness is wanted (one line in `vercel.json`).
- **Open decision:** Porsche admin **Pending** chip is now black — keep, or restore Guards Red as a status signal?
- **Next deliverable (requested this session):** non-technical **one-pagers** for each product — Instinct, Agentic QA, Agentic IAM (OGIAM), Beyond, etc. Confirm the exact product roster + format (hosted one-pager set vs docs) before writing.
- Invoice editing (write-back to SharePoint) is a deferred future layer if the CTO wants parity with job-codes.

---

## Known Blockers

- **Agent session cannot** merge to apex `main`, push to `main`, or read/trigger prod DB/secrets/cron (safety classifier). The CTO merges PRs and triggers authed sweeps; the agent prepares branches + PRs and verifies read-only on the deployed URL.
- Porsche admin OS is **not on `main`** (PR #16 open); the prod domain serves the branch via manual `vercel --prod`, so those changes ship the moment they're deployed, independent of the PR.

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/wolfpack-apex
git pull
npx jest --no-coverage
```
