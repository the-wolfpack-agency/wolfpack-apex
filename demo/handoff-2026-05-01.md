# Wolfpack Instinct — Session Handoff
**Date:** 2026-05-01
**HEAD commit:** `58a039d`
**Deployed:** https://wolfpack-instinct.vercel.app
**Repo:** the-wolfpack-agency/wolfpack-apex
**Note on session continuity.** The original conversation crashed and this doc was reconstructed from git history. After the recovery, three additional follow-ups landed (see "Post-handoff follow-ups" below). All commits up through `58a039d` are pushed and deployed.

---

## Headline

Operating Principles platform shipped end-to-end (13 PRs), Cost Budget platform shipped (1 PR), per-user customizable nav shipped (1 PR), three carryover threads from Apr 30 closed (QR per-scan attribution, Outlook animated signatures, signature-detect 400). 20 PRs merged today (#73 – #92), 8 new migrations (112, 114, 115, 116, 117, 118, 119, 120), ~17.8K LOC added. All on `main`, all on Vercel.

Side stream: 6 PRs on `wolfpack-aidan-mulready` (v2 copy + 5 fixes) — that repo is independent, separate `git push`.

Full release report at `demo/release-report-2026-05-01.md`.

---

## Where to start tomorrow

0. **Verify the badge optimization on the live site.** Open the dashboard, leave it idle 60s, watch DevTools Network. You should see ~8 reqs/min idle (was ~52). Also verify Outlook email + Teams badges still update on real activity. If anything regresses, set `NEXT_PUBLIC_INSTINCT_BADGE_OPTIMIZE=false` in Vercel for instant rollback (no redeploy needed beyond the env-var update).

1. **Verify deploys landed.**
   ```
   gh run list --repo the-wolfpack-agency/wolfpack-apex --limit 5
   gh run list --repo the-wolfpack-agency/wolfpack-aidan-mulready --limit 5
   ```
   Vercel deploy status is on each PR.

2. **Confirm the 8 migrations ran.** `npm run vercel-build` calls `migrate.mjs` automatically. Verify by:
   - Opening `/principles` — if the page loads with the empty state, migrations 116/117/118/119 are in.
   - Opening `/programs/budgets` — if the page loads, migration 120 is in.
   - Opening `/qr` and clicking a scan — if the detail panel renders, migration 112 is in.
   - Opening Settings → Nav preferences — if reorder works, migration 115 is in.

3. **Smoke-test the Principles platform.** This is today's biggest surface and Hoxsie will exercise it:
   - Settings → Principles → paste the SharePoint URL → click Sync.
   - Verify principles appear at `/principles`.
   - As a leader, open `/principles` → click into a team member → drill down on an observation → confirm evidence shows the metric value + `observed_at` timestamp.
   - Edit a principle in-app → verify it re-evaluates the org (the leadership scoreboard updates without waiting for the next 2-hr sync).
   - Wait for / manually trigger the weekly-report cron and confirm the leadership banner surfaces it Monday morning.

4. **Smoke-test the Cost Budget platform.**
   - `/programs/budgets` → Create.
   - Add line items.
   - Export xlsx → open in Excel → modify a line → re-import.
   - Confirm round-trip preserves the WPA template formatting (named ranges, tab order, formulas).
   - Test fixture is at `test-fixtures/wpa-cost-budget-template.xlsx` if you need a known-good input.

5. **Pick up the open threads.** See "Open items" below.

---

## What shipped today (summary — full detail in release report)

### Operating Principles (13 PRs, #78–#90)
- SharePoint .docx → JSZip+regex parser (replaced mammoth in #86)
- 5 real validators wired to live data: `calendar.focus_block_ratio`, `mail.after_hours_send`, `tasks.overdue_rate`, `goals.kr_measurability`, `code.pr_cycle_time_under`
- Schema: migrations 116/117/118/119
- Cron: `/api/cron/principles-sync` (every 2hrs), `/api/cron/principles-evaluate`, `/api/cron/principles-weekly-report` (Mondays)
- UI: `/principles` (member view + leadership scoreboard), `/principles/team/[userId]` (drill-down)
- Self-service config: paste SharePoint URL in-app, no env var, no redeploy
- Native CRUD: create / edit / retire principles in-app without SharePoint
- Per-row `observed_at` + metric value in evidence drill-downs
- Hoxsie explainer at `docs/principles-explainer.md` (paste-ready into the SharePoint doc)

### Cost Budget platform (#92)
- Migration 120 (3 tables)
- WPA xlsx round-trip (`src/lib/programs/budget-xlsx.ts`, 541 LOC)
- 8 API routes, 2 UI pages
- 35 test cases

### Customizable left nav (#76)
- Migration 115
- Per-user reorder + hide
- Action-item emails deep-link to nav targets

### Closed yesterday's open threads
- **`c4dc20a`** — QR per-scan attribution detail view + migration 112 (was untracked at end of Apr 30)
- **`e11d9ac`** — Import animated signatures from Outlook (yesterday's `email-signatures-detect.ts` plumbing now has a UI)
- **PR #73** — graph 400 on signature detect + mobile compose buttons + hide `/sites` from nav
- **PR #75** — preserve body when signature stripper would zero it

### Other
- **#74** — Client toast copy fix (created/updated/deleted)
- **#77** — Lint baseline restore (ignore `.claude` worktrees, suppress unstable rules)
- **#91** — Insights Quick Actions normalizer accepts bare page names

---

## Architecture decisions worth remembering

- **Operating Principles is bidirectional.** SharePoint .docx is the source of truth, but in-app CRUD also writes to `instinct_principles`. Sync cron never overwrites in-app authored principles unless slugs collide.
- **JSZip+regex > mammoth for SharePoint .docx.** mammoth choked on Hoxsie's real file; ~150 LOC hand-rolled extractor handles every doc we've thrown at it. Pattern: prefer minimal hand-rolled parsers over heavyweight parser libs for constrained input formats. (Same lesson as yesterday's `qrcode-generator` swap.)
- **Validator framework is `(userId, principle) => Observation[]`.** One file per validator in `src/lib/principles/evaluators/`. Drop a file + register its `id` in `built-in-validators.ts`. No framework boilerplate.
- **Org-wide fan-out on every edit.** When a leader edits a principle in-app, we synchronously re-evaluate the entire org against it — chosen for instant scoreboard feedback over throughput. Will need to revisit if org grows >100.
- **Per-row `observed_at` is mandatory.** Flattening to "latest observation" makes trend detection ("this principle is being ignored for 3 weeks") impossible. Always store every observation, roll up at read time.
- **WPA xlsx is a real schema.** `budget-xlsx.ts` knows named ranges, tab order, formula columns. Round-trip preserves all of them — Hoxsie's existing Excel workflow is unbroken.
- **Self-service config > env vars.** Migration 119 + the in-app config pattern should be replicated for any future "needs a URL" feature.

---

## Post-handoff follow-ups (same day, after the crash recovery)

Three follow-ups landed after the original handoff was reconstructed:

### 1. AgenticQA repo hygiene (commit `96877c2`, NOT pushed yet — branch `fix/principles-lenient-markers`)

The AgenticQA parent repo at `/Users/nicholashomyk/mono/AgenticQA/` was tracking a 1,194-file embedded copy of `wolfpack-auto/`, causing phantom dirty state every time the canonical repo moved. Fixed by `git rm -r --cached wolfpack-auto/` + `.gitignore` additions for `wolfpack-auto/`, `wolfpack-apex/`, `.vercel/`. The standalone `wolfpack-auto` repo also got a small cleanup (commit `9e38b50` on `the-wolfpack-agency/wolfpack-auto`, pushed): reverted accidental `public/sw.js` eslint-disable removal, added `.claude/` to `.gitignore`. **Decide tomorrow:** push the AgenticQA branch or leave for review.

### 2. Apex → Instinct rename plan + audit tool (commit `4a6ed2e`, pushed)

Tier 4 DB rename (36 tables) was already done Apr 20. Remaining surface is small:

- **130 immutable** files (SQL migrations + dated handoffs — never touch)
- **14 active-schema** files (TS source with `apex_*` table refs hitting compat views — codemod-able)
- **6 active-type** files (configs + top-level docs — hand edit)
- **120 cosmetic** (old handoff docs — skip)

Audit tool: `scripts/audit-apex-refs.sh`. Plan: `docs/apex-to-instinct-rename-plan.md`. Phase A is one PR (~30 min) once you're ready to click the GitHub repo-rename button. Fully reversible at every step.

### 3. Badge polling efficiency pass (commit `58a039d`, pushed)

Idle traffic dropped from ~52 reqs/min to ~8 reqs/min (~85% reduction). Microsoft Graph endpoints unchanged — pure client-side dedup + cadence. Full design at `docs/badge-poll-optimization.md`.

Files added/modified:
- New: `src/lib/coalesced-fetch.ts` + tests, `docs/badge-poll-optimization.md`
- Modified: `src/lib/hooks/useAdaptivePoll.ts` (defaults 5s/45s → 30s/120s; new `idleMs` 180s + `isStable` opt-in), `src/lib/analytics.ts` (new `system.badge_poll_optimized` event), 4 badge components (1-line import swap + stability hint), 3 badge test suites (5s → 30s timing).

Rollback: `NEXT_PUBLIC_INSTINCT_BADGE_OPTIMIZE=false` (Vercel env, instant). No DB changes.

Tests: 64/64 in target suites green; baseline 4,093 tests unchanged; zero regressions.

**Future work (deferred per user):** Microsoft Graph subscriptions → SSE push to eliminate polling entirely. Idle traffic → ~0. 1–2 day effort, separate session.

---

## Open items / what's next

### High priority

1. **Principles weekly report → SharePoint write-back.**
   The explainer (`docs/principles-explainer.md`) promises Instinct generates `Wolfpack — Last Week vs Operating Principles.docx` in the same SharePoint folder every Monday. The cron at `/api/cron/principles-weekly-report` builds the report data and stores it in `instinct_principle_weekly_reports`, but the SharePoint write-back leg isn't wired yet — only the in-app view at `/principles` (leadership banner) shows the report. Resume:
   - Add `src/lib/principles/sharepoint-write.ts` that takes the latest weekly report row and PUTs a generated .docx to the same `siteId` + `driveId` + parent folder used by `sharepoint-fetch.ts`.
   - Generate the .docx with the same JSZip approach used for parsing — not mammoth, not docx-js. Single-page Letter, ranked observations per principle, top-3 evidence rows.
   - Append to the cron handler at `src/app/api/cron/principles-weekly-report/route.ts`.
   - Add `assistant.principles_weekly_doc_written` to `InstinctEventType` for analytics.
   - Test against the actual SharePoint instance — never claim done from mocks alone (per `feedback_no_silent_data_loss`).

2. **Outlook signature import UX polish.**
   `e11d9ac` shipped the import flow but the detected signature renders as raw HTML in the Settings card. Add a styled preview pane + a "looks wrong, re-detect from older messages" button.

3. **Principles UI mobile mode.**
   Leadership scoreboard table is cramped on phones. Card-mode breakpoint at < 640px. The bulletin board association picker pattern from PR #72 (centered bottom sheet on mobile) is a good template.

### Medium priority

4. **Cost Budget actuals recurring sync.**
   Today is one-shot CSV/xlsx import. Wire a cron + a "connect QuickBooks" path so actuals flow in automatically.

5. **Principles validator coverage expansion.**
   5 validators today cover calendar/mail/tasks/goals/code. Gaps relative to the explainer's surface table:
   - Teams chat (response time, mention sprawl, after-hours)
   - OneDrive/SharePoint (doc freshness, ownership, sharing scope)
   - Azure DevOps (sprint completion, build success, deploy frequency)
   - Azure Cost Management (spend trend, anomalies)
   - Entra ID (sign-in anomalies, MFA, privileged role usage)
   Each is a single new file in `src/lib/principles/evaluators/`.

6. **Principles authoring assistance in-app.**
   Native CRUD shipped (#88), but the textarea is unguarded. Add a live-preview pane that shows the parsed structure (markers Instinct picked up) and warns when a `**Signal:**` doesn't match any registered validator id.

### Low priority / nice to have

7. **Re-enable lint rules suppressed in #77** — the two unstable rules need investigation; they fire on legitimate code in some files. Best done in a focused session where we can run `npm run lint -- --rule <id>:error` and read the failures.

8. **`/sites` re-enable.** PR #73 hid it from nav (it's not user-ready). Decide: finish the prompt-editor v2 (was the Apr 17 evening focus), or formally retire `/sites`.

---

## Carryover from before today (NOT touched today, still pending)

The wolfpack-auto repo at `/Users/nicholashomyk/mono/AgenticQA/wolfpack-auto` has uncommitted changes from **2026-04-29** that were never landed:

- `src/app/admin/oem/dealers/page.tsx` (modified)
- `src/app/admin/oem/dealers/DealerRow.tsx` (untracked, but referenced by the page change)
- `src/components/BrandingForm.tsx` (modified)
- `tests/e2e/branding-refresh.spec.ts` (untracked)

These are part of the Apr 29 admin/oem dealer-row + branding-refresh thread. Decide tomorrow: finish + commit, or revert. **Important:** wolfpack-auto is its own repo (`nhomyk/wolfpack-auto`), so `cd` into it before any git operations. Vercel deploys from there, not from the AgenticQA parent.

---

## Wolfpack-Aidan-Mulready (separate repo)

Repo: `nhomyk/wolfpack-aidan-mulready`. 6 PRs landed today (#2–#7). HEAD: `04740e1`.

- v2 copy refresh (CRJ-900 hero, expanded pilot bio, DPN A permit) + Playwright contract guards
- Hero retina swap (low-res → 2560x1082)
- h1 + Partner With Us → /contact (unblocks Playwright)
- Pilot panel min-height clamp
- Hide /admin/images from nav
- Media pack zip wired to a real press zip
- Footer self-URL removal
- `docs(contributing)` doc for Max's PR + Vercel preview workflow

E2E suite at `tests/e2e/`: admin-heatmaps, admin-images, admin-insights, designer-index, driver-pilot-toggle, footer-social, home, media-pack, side-pages. No outstanding work flagged.

---

## Process notes from today (worth remembering)

- **17.8K LOC across 20 PRs in one day.** Driven by tight PR scoping + the validator-as-single-file pattern. Don't change this — it's working.
- **The "swap a heavyweight lib for ~150 LOC of regex" pattern hit twice this week** (qrcode-generator → custom-then-back, mammoth → JSZip). When a parser/encoder lib chokes on real input, default to writing the minimal version before bringing in a second lib.
- **Self-service config (paste a URL) shaves a deploy cycle off every "needs a URL" feature.** Replicate aggressively.
- **The crash that ate the conversational handoff context** is a reminder to write handoff docs incrementally, not at session end. For tomorrow: write the handoff stub at first major milestone, append as PRs merge.
- **Codify scans before AI-grepping.** When the user asked about apex references, the first move was to write `scripts/audit-apex-refs.sh` and get exact bucket counts (130/14/6/120) — not to start grepping in chat. Same pattern: when the user asked about idle polling, static analysis of `setInterval` + `useAdaptivePoll` callers gave the precise math (52 → 8 reqs/min) before writing any code.
- **Phantom dirty state has a cost.** The 1,194-file AgenticQA tracked copy of `wolfpack-auto/` was a low-grade tax on every `git status`. The cleanup is tiny but high-yield. Apply the same lens elsewhere.
- **Always preserve external integration contracts.** The badge optimization deliberately did NOT change any Microsoft Graph endpoint — same scopes, same auth, same queries — to keep Outlook safe. When optimizing, dedup at the client edge before touching the server.
