# Handoff — 2026-04-26 → 2026-04-27

**Reader profile:** Whoever picks this up tomorrow (most likely Nick Homyk continuing this thread). The session focused on getting the porsche-classes automation operator-ready end-to-end against Alicia's real samples.

---

## TL;DR — three things that matter for tomorrow

1. **Vercel daily deploy limit is currently blocking the last fix from going live.** Budget resets in <24h, OR upgrade to Pro. Until then, **Send to SharePoint is broken** with a TypeError, but the fix is on `main` (`e3ee919`).
2. **The pipeline is otherwise verified end-to-end against Alicia's real files.** 16/16 in the local diagnostic harness covering Cognito coordinator + instructor/facilitator/onsite + survey (single + multi-class) + Porsche academy roster.
3. **Operator UI now has Run-now + Backfill buttons** so we can drive the automation without waiting on distribution-list updates.

---

## Where things stand right now

### Code

- `main` HEAD: `e3ee919 fix(automations): SharePoint upload — coerce buildFilename inputs to string`
- One empty redeploy nudge on top: `6eabfeb`
- All commits pushed, no uncommitted work in tree
- tsc clean

### Production

- **Latest live deploy:** `dpl_5hp4Mv64VkvpQSSmLipkSBEk6cNZ` (from `ce27f1d`, debug instrumentation)
- **Stale by:** `e3ee919` (TypeError fix not yet built)
- **Vercel free tier**: hit the 100-deploys-per-24h limit. Webhook is silently dropping new builds.

### Data state

- **Backfill ingest:** Nick uploaded 5 .eml + 5 survey .xlsx + 1 roster .xlsx via the new Operator panel. Most processed cleanly; the multi-file stall bug surfaced and was fixed (`d2deb57`).
- **This week dashboard:** populating correctly with classes parsed from `coreyb 4.20.26.xlsx` (BA101 Apr 20 Ritz, BA102 Apr 27 Four Seasons, BA101 May 4 Four Seasons, BA101 May 4 Ritz, BA102 May 11 Hilton, BA101 May 18 Four Seasons, more below fold).
- **Status icons:** all yellow ("Still waiting on inputs") because the Cognito + survey samples on disk are for *older* classes (Apr 13 Westlake/Orlando, Mar 23 Pendry) — those rows would show their icons lit if scrolled to.

### Distribution lists

| Stream | Reaches Nick directly? |
|---|---|
| Coordinator Class Report (Cognito) | ✅ Yes (Cognito Notifications opt-in) |
| Instructor / Facilitator / Onsite Class Report (Cognito) | ❌ Not yet — only via Alicia's forwards |
| Porsche academy roster | ❌ Not yet — only via Alicia |
| Survey vendor | ❌ Not yet — only via Alicia |

Until those three lists are updated, the unattended cron poll won't pick anything up from Nick's inbox. The Backfill button is the workaround for end-to-end testing today.

### Vercel env vars

- `AUTOMATION_POLL_USER_ID` — **not set**. Hourly cron will return `skipped: "no_user_connected"`. Set it to Nick Homyk's Instinct user.id once the rest of the pipeline is verified.
- `INSTINCT_SHAREPOINT_SITE_ID` / `_DRIVE_ID` / `_CLASS_SUMMARIES_PATH` — not verified this session; if SharePoint upload returns 202 `not_configured` after the deploy lands, that's the next thing to set.

---

## First thing to do tomorrow

1. **Confirm the deploy went through.** Check Vercel dashboard (https://vercel.com/nhomyks-projects/wolfpack-instinct → Deployments). The "Current" deploy should reference commit `e3ee919` (or later if more landed overnight).
   - If it's still `ce27f1d` and the rate-limit window is past, push a one-character empty commit to nudge:
     `git commit --allow-empty -m "chore: redeploy nudge" && git push`
2. **Repro the Send to SharePoint flow.** Open `/automations/porsche-classes`, click any row that shows the 🎓 (roster) icon lit, click **Send to SharePoint**.
   - **Expected:** ✓ Uploaded, web_url returned, audit row written.
   - **If it still fails:** the new diagnostic block expands under the error banner with `upstream_status / filename / course_type / class_date (raw) / location / byte_count`. Paste that block back into the chat and we patch from there.
3. **If success, set `AUTOMATION_POLL_USER_ID`** to Nick's Instinct user.id so the hourly cron starts polling automatically. Get the user.id from devtools → Application → Local Storage → `instinct_user.id` field.

---

## Open follow-ups (none blocking)

- **Email distribution opt-ins:** Nick to be added to (a) Cognito instructor-form notifications, (b) Porsche academy daily-report list, (c) survey vendor list. Until then, ingestion only works via Backfill (manual upload) or via Alicia's forwarded emails landing in Nick's inbox.
- **Vercel Pro upgrade:** project_production_checklist + project_wolfpack_prerelease both flag this as needed. The 100/day rate-limit hit today is the first time it's bitten this surface; will recur as commit volume grows.
- **Multi-mailbox poller:** Phase-3 work to remove the single-inbox dependency entirely. Deferred — the simpler answer is provisioning a shared `pcna-summaries@thewolfpack.agency` mailbox + setting `AUTOMATION_POLL_USER_ID` to its Instinct user. ~2h of admin setup, no code change.
- **SharePoint env vars:** verify production has `INSTINCT_SHAREPOINT_SITE_ID` / `_DRIVE_ID` / `_CLASS_SUMMARIES_PATH` set. If not, the route returns 202 `not_configured` (graceful skip) instead of an error.
- **Calendar phase 2:** month grid + create-event flow via existing `Calendars.ReadWrite` scope. Deferred.
- **Pre-existing PDF-render test failures:** 5 in `lib/automations/porsche-classes/__tests__/export-pdf.test.ts`, present on main before this session. Unchanged. Worth a separate look but not urgent.

---

## Files of interest for tomorrow

| Path | Why |
|---|---|
| `src/app/api/automations/[automationId]/summaries/[classKey]/upload-sharepoint/route.ts` | The route that's currently broken in production. Fix is in this file already, just unbuilt. |
| `src/app/api/automations/[automationId]/summaries/[classKey]/upload-sharepoint/__tests__/route.test.ts` | 20 tests including 3 regression guards for the Date / nullish / number coercion case |
| `src/lib/automations/porsche-classes/__tests__/diagnose-real-emls.test.ts` | Local-only harness that runs against `~/Downloads/Automating Class Summaries/`, `~/Downloads/Program Evals/`, and `~/Downloads/Automating Porsche Class Participant Updates_Changes/`. 16 cases, all green. |
| `src/app/(dashboard)/automations/porsche-classes/_components/OperatorActions.tsx` | The Run-now + Backfill panel |
| `docs/features/porsche-classes-flow.md` | Non-technical pipeline write-up — share with stakeholders |
| `demo/release-report-2026-04-26.md` | Companion to this handoff |

---

## What got committed by mistake today

- One empty commit landed on `nhomyk/AgenticQA` instead of `the-wolfpack-agency/wolfpack-apex` when the cwd shifted between turns. It's a harmless empty no-op (`3454cc1`), pushed nothing user-visible. Going forward I'll re-anchor `cd /Users/nicholashomyk/mono/wolfpack-apex` at the start of every wolfpack-apex git action — the memory invariant covers this and I'll respect it.

---

## Memory invariants reinforced today (important for next session)

- **Build tooling first, run, analyze.** The `diagnose-real-emls` harness is the gold-standard pattern: real Alicia samples → production parsers → assertions. Saved hours of guessing. Apply this to every new format we encounter.
- **wolfpack-auto/ vs wolfpack-apex/ are separate repos.** This session was wolfpack-apex; wolfpack-auto wasn't touched. Confirm cwd before every git command.
- **Vercel free tier limit is real.** Don't burn deploys on `[skip ci]` baseline pushes — the cron and the operator both share the budget. Pro upgrade is queued.

---

## One thing to celebrate

The end-to-end picture as of the last deploy:

```
Inbox / Backfill upload
  → parser (one of 4: coordinator / instructor / survey / porsche_xlsx)
  → snapshot (Postgres)
  → assembler (one AssembledSummary per class_key)
  → exception detector
  → Review page (Word / PDF / JSON / SharePoint buttons)
  → SharePoint upload (one click)
  → audit row (24h undo window)
```

Every box parses cleanly against real Alicia samples. The only unbuilt link in the chain right now is the Date-coerce hotfix, gated on tomorrow's Vercel rate-limit reset. Once that lands, the operator clicks one button and the entire flow Alicia used to drive across six tools by hand runs in the background.

Good handoff target: leave the dashboard at `/automations/porsche-classes` with the Backfill panel populated. First action tomorrow is one click on Send to SharePoint to validate.
