# Wolfpack Instinct — Session Handoff
**Date:** 2026-05-15
**HEAD commit:** `984c70b`
**Deployed:** https://wolfpack-instinct.vercel.app
**Repo:** the-wolfpack-agency/wolfpack-apex

---

## Headline

Salesforce went from "OAuth handshake works" to a full client-ready CRM surface end-to-end in one session: live-validated against a real Developer Edition org with sample data (United Oil & Gas, Grand Hotels), all eight core CRM operations working — Find by ID/name/email, Drill in, Create (contact/deal/account/task with confirmation), Update fields (search-then-PATCH with safety), Related records ("Acme's opportunities"), Filter queries ("deals over $50k closing this month"), one-click Quick Connect admin UI with verify/disconnect — plus an answer-relevance eval harness, a porsche-classes grounding lane, and a HubSpot validation playbook ready to drop in.

---

## What Shipped Today
_(Commits since the last handoff on 2026-05-14.)_

### Features
- `1393f66` feat(connectors): related-record + filter-query tools (Acme's deals / deals over $50k closing this month)
- `5499c53` feat(connectors): P0 write actions — create + update against Salesforce/HubSpot through Phase-3 confirmation
- `8595406` feat(connectors): one-click OAuth admin UI + verify/disconnect routes
- `3e48b53` feat(assistant): free-text search against CRM connectors (Salesforce SOQL + HubSpot search endpoint)
- `4dd498c` feat(assistant): generic OAuth2 framework for connectors (Salesforce + HubSpot today; Jira/QBO/GitHub plug in)
- `88dd5d6` feat(assistant): porsche-classes as a grounding lane in getRelevantContext
- `0af7001` feat(assistant): eval harness for answer relevance (catches the 2026-05-14 regression class)
- `965f40f` feat(multi-tenant): wire workspace_id through auth, routes, and invite flow
- `eb133dc` feat(assistant): vendor presets + admin UI for connector credentials
- `e45b039` feat(assistant): workspace-aware tool dispatch + admin API for connector creds
- `d1622e7` feat(assistant): per-tenant connector credentials with AES-256-GCM at-rest
- `9f55054` feat(assistant): Phase 4 — connector framework + generic REST + first connector-backed tool
- `8667dc2` feat(assistant): Phase 3 — action tools + confirmation flow + save_team_fact
- `a6db4f5` feat(assistant): drag/drop into chat → permanent Brain ingestion
- `dbb5818` feat(assistant): Phase 2 — 4 read-only tools (calendar / mail / goals / financials)
- `385b6a8` feat(assistant): Phase 1 — tool-calling foundation + get_org_facts
- `a2d4709` feat(assistant): thread Brain hits + topScore into LLM context + quality gate
- `5abd44d` feat(invites): 30-day expiration + admin resend endpoint
- `da20adc` feat(assistant): per-workspace strictness mode + tenant-scoped citation validation
- `2b67095` feat(assistant): client-grade answer-quality filter pipeline

### Fixes
- `a9a2c46` fix(auth): inline requireCapability in invite POST handlers to satisfy static-analysis
- `9134b29` fix(assistant): bake weekday hints into page-facts bypass + extend calendar tool intents
- `f9c7a9d` fix(connectors): auto-route get_external_record to vendor-configured connector + seed objectMap from preset on OAuth save
- `0a1dd9a` fix(auth): requireCapability falls back to access-token cookie when Authorization header missing
- `a8b8def` fix(assistant): KB similarity gate + A1 no-op on zero hits (no more canned-reject for general-knowledge)
- `db26c93` fix(assistant): weekday-aware timeframe resolver + bump deprecated actions/cache pin
- `fa896ad` fix(assistant): prevent future-date hallucinations in Assistant answers

### Docs
- `984c70b` docs: HubSpot OAuth + verification setup (mirror of salesforce-oauth-setup)
- `2872a60` docs: salesforce search live validation log + client query catalog
- `982ea69` docs: handoff + release report for 2026-05-14

### Other
- `882a7aa` chore(deploy): redeploy to pick up SALESFORCE_CLIENT_ID/SECRET env vars
- `5b3aa6a` ci(red-team): pass apply as boolean, not string

---

## Conversational Context (the part git can't see)

- **The vision driving this session: "infinitely powerful, ready for client deploy."** You wanted the assistant to operate as the user's full org-view layer — CRM data + meeting notes + email threads + auto-classes data — and to do most normal SF functions (not just look-up). Every feature this session was framed against that goal, ranked by "what breaks a client demo if it's missing."
- **Live demo SF org credentials are in chat history.** `homyk.6b1ab6d50d8c@agentforce.com` is a Developer Edition org with the OAuth Consumer Key + Secret you posted directly: `3MVG97L7PWbPq6UyOXVMGk7G1BVIjy0o6slUEGhgIjPv7qNiisvaOmYxIQao0HQ2LE9vXApKJY8S9FtZ5096k` / `3F5E85E206E4ED5C0E626F53624D6677C66CEE5046385B7D28E77A8836BFFF41`. For a Developer Edition demo org with no real customer data, low risk — but rotate these via SF Setup → External Client Apps → Manage Consumer Details if anything sensitive lands in the org. The keys are in Vercel env vars (`SALESFORCE_CLIENT_ID` / `SALESFORCE_CLIENT_SECRET`, production scope) and that's how production reads them.
- **The DE org ships with extensive sample data** (United Oil & Gas Corp, Grand Hotels & Resorts Ltd, Burlington Textiles Corp of America, ~10 opportunities ranging $235k–$915k, multiple stages). This is why the live filter-query test was so compelling — `deals over $50k` returned 10 real opportunities ordered by amount. For client demos, ⚠️ note this sample data persists; either mass-delete via SF Setup → Mass Delete Records, or scope queries to specific names. For real clients, only their data appears (per-tenant instance_url).
- **2026-05-14 regression class was the strategic pivot.** "What is Nurburgring?" returning Morning Briefing content + "what meetings do I have Monday" returning today's meetings made you ask "why didn't tests catch this?" — that question is the parent of: the eval harness, the weekday-aware page-facts bypass, the leading-noun calendar patterns, and the regression-cases-in-cases.json convention. Every prod regression now gets a pinned eval case.
- **You picked the multi-workspace switch first** out of three deployment-readiness candidates I proposed, explicitly because "deployment blocker > QoL." That instinct held — once multi-workspace landed, every subsequent connector feature was tenant-isolated by default with no rework.
- **OAuth scope decision: "build the framework generic, not Salesforce-specific."** This shaped every file in `src/lib/assistant/connectors/oauth/` — providers/, registry, refresh orchestrator are vendor-agnostic. Adding a new vendor (QuickBooks, Jira, GitHub, Zendesk) is now a single-file change. You confirmed multi-CRM is the strategy, not just SF.
- **Honest scope conversation about onboarding:** you asked "what's best practice for client onboarding to connect Salesforce" and the answer was the 90/10 split — 90% of clients get one-click OAuth via a Wolfpack-owned SF app; 10% (security-conscious) bring their own OAuth app via per-workspace client_id/secret. Today shipped path A only (shared dev-org OAuth app). Path B is deferred until the first paying client who requires it.
- **You ran every prompt live against the demo SF org as I shipped it** — name search, drill-in, create/update preview, related records, filter queries. Every commit got a live validation pass within minutes. Catching the "rest-default isn't configured" routing bug + the `requireCapability` cookie fallback bug live (not in unit tests) is why this session's quality is so much higher than yesterday's.
- **HubSpot setup is documented but not live-tested.** `demo/hubspot-oauth-setup.md` is the playbook; needs your ~30 min of HubSpot developer account setup + env vars before we can run the same query catalog against HubSpot. Logic is unit-tested at every layer; the live "does HubSpot's recency-sort vs SF's relevance-sort cause issues" test is the open verification.
- **Security Self-Scan failed on 2 NEW critical/high findings** that turned out to be scanner false positives (it couldn't follow `return inviteFlow(req)` indirection to see the requireCapability gate). Fixed by inlining the auth check at the POST handler so static analysis sees it directly. Watch this pattern — the scanner only checks function bodies, not delegated calls.

---

## Open Items / What's Next

**Highest-ROI moves heading into the next session (ranked):**

1. **HubSpot live verification** — the playbook is shipped ([demo/hubspot-oauth-setup.md](hubspot-oauth-setup.md)) but never exercised live. Until it's done, "multi-CRM" is theoretical. Your ~30 min of HubSpot setup unblocks this. Once connected, run the same Salesforce query catalog against HubSpot; document divergences (likely: HubSpot ranks by recency not relevance, lowercase property names, no Opportunity SObject — uses Deal directly).

2. **Aggregate counts** — "how many deals in the pipeline" / "what's my win rate" / "top 5 accounts by revenue". Last remaining piece of the P0 sales-reporting surface. Vendor preset needs a `countQuery.build()` that emits `SELECT COUNT() FROM ...` SOQL; new tool with intent patterns ("how many", "count of", "average"). ~1 hour.

3. **Tool-dispatch transparency UI** — the deferred "#3" from a few sessions back. Expandable line under each assistant answer: `via filter_external_records(amount>50000) · 10 results from salesforce`. Three benefits: (a) instant misroute diagnosis, (b) generates eval cases automatically when user marks an answer wrong, (c) audit trail for client-facing demos. ~1 hour.

**Deferred items still tracked:**

- **HubSpot write actions live-test** — `create_external_record` works against Salesforce; the vendor preset has HubSpot's `{ properties: ... }` envelope for create + update. Logic is unit-tested but the live HubSpot POST has never fired. Bundle with #1 above.
- **Update tool field-name normalization for HubSpot** — current alias map (`stage → StageName`, `email → Email`) is Salesforce-shaped. HubSpot wants `firstname` / `lastname` / `email` lowercase. Per-vendor alias layer needed once HubSpot writes go live.
- **Filter queries for non-Salesforce vendors** — HubSpot has a different filter API (POST `/crm/v3/objects/<type>/search` with filter body). Vendor preset's `filterSearch.build()` needs a HubSpot implementation. Today only Salesforce.
- **Custom SObject support** — `vendor-presets.ts` only knows about standard Contact/Opportunity/Account. Clients with `CustomObject__c` need a per-tenant objectMap override on the admin UI. Not blocking today, blocking for any client with custom SF objects.
- **Per-workspace OAuth credentials** (the 10% path from the onboarding-strategy conversation) — for security-conscious clients who can't allow a 3rd-party OAuth app. Extends the connector_credentials schema with `oauth_client_id_enc` + `oauth_client_secret_enc` columns. Defer until needed.
- **Refresh-on-401 live validation** — only unit-tested. Forcing a refresh via Salesforce's UI was hard to find. Could ship a CTO-only `/api/admin/connectors/[name]/force-expire` debug endpoint that sets `access_token_expires_at` to the past — ~10 min build, gives us deterministic refresh testing forever.

**Pipeline state:**

- AgenticQA Full Pipeline triggered on `a9a2c46`: https://github.com/the-wolfpack-agency/wolfpack-apex/actions/runs/25936364673 — verify green when it finishes.
- Security Self-Scan should be green on the next push after `a9a2c46` (the false-positive auth findings are now inlined).

---

## Known Blockers

- **Wolfpack-owned production OAuth app needed for client deploy.** The current Salesforce OAuth app lives in your personal Developer Edition org (`homyk.6b1ab6d50d8c@agentforce.com`). DE orgs have a hard-cap of a handful of external connections + the app dies if you stop logging in every 6 months. Before first paying client: move the OAuth app to a Wolfpack Partner/Developer SF account. Same story for HubSpot once it's wired.
- **Salesforce AppExchange Security Review** — only blocks AppExchange listing (~4–6 weeks process). For private distribution to specific client orgs we can skip it; SF allows up to a few hundred installs without the review. Note this when planning the first multi-client rollout.
- **Pre-existing flaky test suites: ~27.** Same set as yesterday — `routes-exist`, `page-facts`, `dashboard-pages-render`, `EmailNavBadge`, `automations/meeting-insights/extractors`, etc. Pass in isolation, fail in the chained run. Looks like jest-mock state leak. Already flagged in yesterday's handoff; still blocking the green-on-every-PR promise we'd want before a paying tenant.
- **Sample data in the demo SF org will appear in unscoped queries.** "deals over $50k" returned 10 United Oil / Grand Hotels / Burlington results because the DE org ships with them. For client demos either mass-delete via SF Setup → Mass Delete Records, or use scoped queries ("Grimace's deals" instead of "all deals"). Documented in [demo/salesforce-search-validation-2026-05-15.md](salesforce-search-validation-2026-05-15.md).
- **AgenticQA-core reusable-workflow coupling.** Several CI workflows in this repo call into the-wolfpack-agency/AgenticQA-core at pinned SHAs. When AgenticQA-core breaks, every consumer fails simultaneously. Same coupling risk flagged yesterday; no fix yet.

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/wolfpack-apex
git pull
npx jest --no-coverage
```
