# Wolfpack Instinct — Session Handoff
**Date:** 2026-07-11
**HEAD commit (main, deployed):** `05947741` (#153) — verified serving on prod via `/api/version`
**Deployed:** https://wolfpack-instinct.vercel.app
**Repo:** the-wolfpack-agency/wolfpack-apex

---

## Headline

Shipped the **agent-evals + deployment-observability arc**: model-version regression detection (#151), the full **deployment pipeline view** with a per-agent Deployments tab (#152), and the **deploy → agent-quality correlation** (#153) — then wired Instinct's vector store to the **existing Qdrant cluster** (zero code) and course-corrected hard on provisioning after confirming Microsoft/email were already live and the "dead" `rag-providers` layer is actually **parked, tested Azure-migration scaffolding — do not delete**.

> Scope note: "What Shipped Today" is auto-generated as *everything since the last handoff (2026-07-07)* — that's the full deploy-control-plane + agent-evals + constitution arc (`#137`–`#153`). All of it is merged to `main` and verified live on prod (`/api/version` = `05947741`).

---

## What Shipped Today
_(Commits since the last handoff on 2026-07-07.)_

### Features
- `439bef7e` feat(deploy): correlate the live deploy with agent model regressions
- `7600e3d4` feat(deploy): full deployment pipeline view (deploy control plane, phase 3) (#152)
- `f0013e60` feat(agents): model-version regression detection (agent evals, phase 1) (#151)
- `7f621863` feat(agents): surface the Production Release Gate on the agents page (deploy control plane, phase 1) (#149)
- `c4868d15` feat(agents): governed LLM reasoning fallback (agents no longer fail at no_match) (#147)
- `13dfabe7` feat(agents): screenshot capture as agent verification output (#146)
- `1672d7f0` feat(maintenance): intake + telemetry rails for the daily bug/feature queue (#137)
- `99332902` feat(invoices): read-only PCNA invoice tracker mirroring SharePoint (#133)

### Fixes
- `3840a85b` fix(hr): re-adding a removed person reactivates instead of failing (#148)
- `59ce1ebc` fix(tasks): don't force a list pick — default to the user's default To Do list (#145)
- `a049e2e2` fix(deploy): remove duplicate release-gate-check cron (unblocks prod deploy) (#139)
- `76f7dd3a` fix(deploy): re-schedule the release-gate notifier cron + restore its guardrail (#138)
- `99e89541` fix(deploy): schedule the release-gate notifier so deployment activity hits the bell (#135)
- `bb0b560f` fix(invoices): land the hub rework that #133 merged without (#134)

### Docs
- `261b3034` docs: 2026-07-07 release report + handoff (Invoice Tracker + deployment notifications) (#136)

### Other
- `f323d725` Deploy control plane #2: dispatch an agent to triage a blocking deploy (#150)
- `39da049d` chore(constitution): sync to v1.1.0 (mandatory E2E UI verification per feature) (#144)
- `69a3564c` Agent control plane: run-task template on the detail page + assistant chat widget (#143)
- `d478a0c5` Govern the assistant + OGIAM agents with the OGIAM Agent Constitution (#142)
- `cef27215` ci: hands-off Dependabot auto-merge (CI + prod-health gated, humans still reviewed) (#141)
- `a3ec43af` deps(deps): bump bare-os from 3.8.7 to 3.9.3 (#18)
- `88023024` deps(deps-dev): bump node-releases from 2.0.37 to 2.0.50 (#16)
- `dad1455c` deps(deps-dev): bump call-bind from 1.0.8 to 1.0.9 (#15)
- `e7c8f9e1` deps(deps-dev): bump ajv from 6.14.0 to 6.15.0 (#13)
- `3d6b191f` ci(deps): bump the actions-ecosystem group with 3 updates (#132)

---

## Conversational Context (the part that matters tomorrow)

**The strategic arc (validated, then built).** After an analysis session, the CTO validated the direction "**agent evals + fleet observability**" as Instinct's next big bet (the founding pain is "agents behave inconsistently across model/agent versions"). Shipped in order:
- **#151 model-version regression detection** — the differentiated net-new: every prior eval/drift check compares across *time*; nothing keyed on *model version*. New `src/lib/agents/evals/*` reads `agent.task_completed` (which already carried `model_id`), computes per-model success rate, compares newest vs prior model, persists to `instinct_agent_model_regressions` (migration **221**). No auto-pause (unlike drift) — a model regression notifies the owner for a rollback decision. Daily cron `0 5 * * *`.
- **#152 deployment pipeline view** — `src/lib/deploy/pipeline.ts` stitches CI→merge→build→promote→verify→health per change by *composing existing readers* (release-gate GitHub GraphQL + `src/lib/integrations/vercel.ts` + `/api/version` serving-sha + readiness). New `Stepper` console primitive. Surfaced on `/admin/deployment` (fleet) AND a new **Deployments tab** on agent detail. Read-only; promotion stays the release gate's action.
- **#153 deploy → agent-regression correlation** — the cross-data payoff: on the LIVE deploy, show model regressions flagged *since it went live* (`listModelRegressionsSince`). Framed as temporal correlation (rollback signal), **not** causation. Appears only once the daily sweep flags a regression (needs agents run across 2+ model versions first).

**Provisioning (the advisory-10 on `/admin/deployment` readiness) — RESOLVED / corrected:**
- **Qdrant: DONE.** The CTO reused the **existing `wolfpack-prod` Qdrant cluster** (created a full-access API key, set `QDRANT_URL` + `QDRANT_API_KEY` in Vercel, redeployed). **Zero code** — the live vector path (`src/lib/brain/qdrant.ts` → `apex_brain`, real OpenAI 1536-dim embeddings) reads exactly those vars. If sharing the cluster risks a collection-name clash, `BRAIN_QDRANT_COLLECTION` overrides it.
- **Microsoft Graph + Resend: already live** — mail/calendar/tasks and email were already configured (I wrongly listed them as TODO; the CTO corrected me — they're green on readiness).
- **Neo4j: deliberately DEFERRED, do not provision.** It's write-only in Instinct: `recordKnowledgeInteraction` writes, but `getKnowledgeGraph` (the only reader) has **0 callers**. Triple-write degrades to Postgres-only, app behaves identically. Provision it only when a feature actually *reads* the graph. Trial expired anyway = would cost money for zero value today.

**⚠️ CRITICAL TRAP — do not repeat my mistake.** I twice recommended provisioning/building things that already existed, and once proposed a "cleanup PR to delete dead code." **That code is NOT dead:**
- `src/lib/rag-providers/*` (Azure AI Search / AGE / dual-write provider abstraction) — **parked, tested (97 tests), documented Azure-migration scaffolding**. See **`docs/azure-migration.md`** (phases: shadow → dual-write → cutover) + commit `81c7a257` "Azure provider scaffolding." Factory branches `throw` because adapters land in follow-ups — that's *unwired*, not dead.
- `neo4j.ts` `getKnowledgeGraph`/`recordCollaboration`/`recordDocGeneration` and `src/lib/ai/semantic-cache.ts` — all have passing test suites.
- **Lesson the CTO drove home:** `grep "who imports this"` tells you what's on the *runtime path*, not what's *dead*. Dead-vs-parked is answered by **tests + docs + git intent**. **Verify against what already exists (live UI, tests, docs) BEFORE recommending provisioning, building, or deleting. Reuse-first. Never repeat process/code.**

---

## Open Items / What's Next

- **Nothing left to provision.** Qdrant done; Microsoft/email live; Neo4j deferred by decision. The readiness page (CTO-authenticated) is the source of truth for any remaining red row — ask, don't guess.
- **Verify #153's correlation line on prod** once agents have run across two model versions and the daily eval sweep (`0 5 * * *`) has flagged a regression. Until then the "regressions since this deploy" line is correctly absent.
- **Parked next-big-thing: the Azure vector/graph migration** (`docs/azure-migration.md`). The scaffolding + 97 tests exist; it needs the adapters *wired* into the live write path (`triple-write.ts` + `brain/*` currently call Qdrant directly). This is a real, deliberate build — NOT a config flip — do it on purpose if consolidating onto Azure, not as a side effect.
- **Minor DRY (optional):** `evals/store.ts` `runModelEvalSweep` and `drift/store.ts` `runDriftSweep` share near-identical "iterate active agents + tally" boilerplate; a shared helper would remove the duplication (logic is distinct, only scaffolding overlaps).
- **Do NOT** open a "delete dead code" PR for `rag-providers` / neo4j read fns / `semantic-cache` (see the trap above).

---

## Known Blockers

- **Agent session cannot** merge to apex `main`, push to `main`, set/read prod secrets, or trigger prod cron (safety classifier + GitHub protection). The CTO merges PRs, sets Vercel env, and triggers authed sweeps; the agent prepares branches + PRs and verifies **read-only** on the deployed URL.
- **Provisioning needs the CTO's external accounts** (Qdrant/Azure/Neo4j creds) — the agent can't create them. Always check whether a service is *already provisioned/live* before recommending setup.
- The authenticated `/admin/deployment` readiness page and any authed API are not reachable by the agent (no prod creds); the agent can only hit public endpoints like `/api/version`. Confirmations of authed surfaces require the CTO's eyes or a stubbed-session render check.

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/wolfpack-apex
git pull
npx jest --no-coverage
```
