# Wolfpack Instinct — Session Handoff
**Date:** 2026-04-16
**HEAD commit:** `d0427b7` (will bump after handoff commit)
**Deployed:** https://wolfpack-instinct.vercel.app
**Repo:** the-wolfpack-agency/wolfpack-apex

---

## Headline

`/sites` is now usable end-to-end for non-technical users (Max + Meghan): every site can be archived, the detail page is a guided 1→2→3 flow with a status banner that tells the user the next step, and any expired session inside the dashboard now silently refreshes its JWT or redirects to login instead of rendering a blank "Unauthorized" page. **Verified live on prod (`e82d71f`)** — all 8 redesign strings confirmed in the deployed JS bundle. Vercel deploys for the first two attempts (`d0427b7`, `e4fd772`) had silently failed because the `fetchWithRefresh` migration shoved `"use client"` to line 2 of 9 components; `next build` is now Stage 4 of `scripts/verify.sh` so the class can't recur.

---

## What Shipped Today
_(Commits since the last handoff on 2026-04-15.)_

### Features
- `d0427b7` feat(sites): delete site + redesigned detail page + dashboard auth-redirect — covers all 4 client-reported `/sites` issues (no delete, confusing detail page, view-flow, expired-auth blanking).
- `e76b210` feat(auth): proper refresh-token flow client-side — `fetchWithRefresh` + guardrail test (foundation that today's mass migration sits on).

### Test infrastructure
- `src/lib/__tests__/sites-delete.test.ts` — DELETE route contract (401 / 404 / 200 + audit row).
- `src/lib/__tests__/sites-detail-ui.test.tsx` — `statusCopy()` regression suite (7 cases). Full page-render UI test deferred to Playwright (see "Open Items").

### Conventions caught up
- `site.archived` registered in `ApexEventType` (per `.ai/conventions.md` event-naming rule).
- 45 dashboard pages + components migrated from raw `fetch("/api/...")` to `fetchWithRefresh` (per `.ai/conventions.md` "Client-side authenticated fetches" section). `connect.ts` integration helpers also migrated.

---

## Conversational Context (the part git can't see)

- The user explicitly called out that I shipped the morning commit (`d0427b7`) **without** reading `wolfpack-apex/CLAUDE.md` or `.ai/*` first. I had wolfpack-auto's CLAUDE.md loaded in context (because I started the session there before pivoting to Instinct) and assumed it covered Instinct. It does not — Instinct has its own conventions (capability-coverage, audit-coverage, `requireCapability` pattern) that I missed. **The pre-existing 7-test-failures baseline is not OK long-term**; it's just what was true when I arrived.
- This handoff and the `statusCopy` extraction were done as remediation after the user pushed back. The principle to take into the next session: **read the repo's CLAUDE.md + .ai/ before acting**, even if a sibling repo's docs are already loaded.

---

## Open Items / What's Next

- **Playwright for Instinct.** `.ai/conventions.md` line 70 says *"add E2E when the repo grows one"* — that day arrived. The sites detail page redesign was UI-driven; the only reason it shipped without a full UI test is `use(params: Promise)` doesn't unwrap inside RTL's `waitFor` scheduler. A Playwright suite hitting the deployed URL is the right home for the guided-flow test (load → edit → save → deploy → poll → preview iframe → archive → back to /sites) and several other UI surfaces.
- **Pre-existing 7-test failures** to triage — none are mine, all predate today, all match the 2026-04-15 baseline:
  - 4 in `audit-coverage.test.ts`: Teams routes (`online-meetings`, `teams/channels-sync`, `teams/teams/[id]/channels/[channelId]/messages/[messageId]/replies`) need either `recordAudit` calls or AUDIT_ALLOWLIST entries.
  - 1 in `verify-script.test.ts`: expects `[FAIL]` / `[PASS]` formatting that `scripts/verify.sh` doesn't emit. Either align the script or relax the assertion.
  - 2 elsewhere (need to be re-grepped).
- **210 ESLint findings** (111 errors, 99 warnings) — all pre-existing. `verify.sh` Stage 1 has been red on baseline. Worth a focused cleanup pass.
- **DELETE route uses `getUserFromRequest` not `requireCapability`** — matches the existing GET/PATCH siblings on the same route, but `.ai/conventions.md` step 1 is "pick or add a capability." A `sites.archive` (or `sites.write`) capability + retrofit of the whole `sites/[id]` route handler family is the right follow-up.
- ~~**Verify on the deployed URL.**~~ Done in commit `e82d71f`. Caught two failed Vercel builds (`d0427b7`, `e4fd772` — `"use client"` directive was on line 2 of 9 components after the migration), fixed the directive position, added `next build` to verify.sh as Stage 4. Reproduced on prod by curl'ing the chunk bundle and grep'ing for the 8 new strings — all present. Next-step verification (manual click-through with auth) still owed if you want eyes-on-glass confirmation.

---

## Known Blockers

- `INSTINCT_JWT_SECRET` (≥32 chars) — code throws in production without it.
- `RESEND_API_KEY` + sender domain.
- `GITHUB_TOKEN_WOLFPACK_AGENCY` + `WOLFPACK_SITES_WEBHOOK_SECRET` for sites deploys.
- `PROD_DOMAIN` env var to activate the TLS hybrid posture CI assertion.

---

## Test State

- 1796 / 1804 passing, 1 skipped, 7 pre-existing failures (matches 2026-04-15 baseline; nothing introduced today).
- 0 new TypeScript errors on touched files. (Pre-existing TS errors remain in `sites-assets.test.ts`, `sites-e2e.test.ts`, `teams-channels-api.test.ts` — TypeScript 5.x `Uint8Array<ArrayBufferLike>` regression + Next 16 Promise-shape param drift.)
- ESLint clean on every file touched today.

---

## How to Resume

```bash
cd /Users/nicholashomyk/mono/wolfpack-apex
git pull
npm run verify          # canonical pre-push (lint + tsc + jest)
# Live preview after Vercel deploy:
open https://wolfpack-instinct.vercel.app/sites
```
