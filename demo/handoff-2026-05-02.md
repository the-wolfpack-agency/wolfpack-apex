# Handoff — 2026-05-02 (end of session)

**Reader profile:** Whoever picks this up Monday — most likely Nick Homyk continuing this thread. Today's session built out the principles platform end-to-end (validators + Microsoft sign-in + team identity + SharePoint write-back) but ended with one user-visible gap that requires Hoxsie's manual action.

**Read these first:**
- `demo/release-report-2026-05-02.md` — full inventory of what shipped + the day-end honest assessment of where the deploy churn came from.
- The previous handoff at `demo/handoff-2026-05-01.md`.

---

## TL;DR — three things that matter Monday

1. **Hoxsie has to log out + sign in via the Microsoft button** at `https://wolfpack-instinct.vercel.app/login` to fix his empty My-principles tab. His current session is on the migration-001 demo CEO seed (`demo-ceo` / `ceo@wolfpack.dev`), not his real Microsoft identity. The data IS there; it lives under his real user id (`bef8a32d-…`) which only loads when he's authenticated as the real him.
2. **App-only Graph access is currently OFF** (`INSTINCT_GRAPH_APP_ONLY=false`). Foundation shipped today (helpers, validator orchestrator, mail-after-hours converted). Don't flip it back on until Azure tenant-admin Application-permission consent is verified-with-effect AND the probe-and-fallback pattern lands (queued for next session).
3. **Don't ship UI bugfixes on theory.** Today's worst hour came from speculating about why Hoxsie's tab was empty across 8 deploys before getting the localStorage value that revealed the demo-account login. New permanent rule in memory: no code ships for a UI bug that doesn't reproduce in tests until the real prod state is observed (localStorage, API response, env var, DB row).

---

## Where things stand right now

### Code

- `main` HEAD: `8f08208 fix(principles me): canonicalize via DB CTE instead of JWT email/name match`
- All commits pushed.
- Vercel: deployed and green.
- 31 commits today (incl. 14 chore baselines from CI).
- 9 migrations: 121, 122, 123, 124, 125, 126, 127, 128, 129.

### Production state

- Principles table: 10 active principles, all 10 user's manual entries restored after the morning incident.
- Team-members table: 8 active rows after migration 128 dedup (Hoxsie, Homyk, Jorge, Max, Meghan, Alicia, Ashley, David). UNIQUE-on-LOWER(email) constraint locks future inserts.
- ms_tokens: 2 connected (Homyk, Hoxsie's real account). Other 5 seats need to sign in.
- Observations: TRUNCATEd at session end as part of migration 124/128 cleanup; the next periodic cron OR a manual "Run all" click repopulates against the corrected validators.

### Coverage banner

Reads "2 of 8 team members connected — not yet: Alicia Zulker, Ashley, David, Jorge Colon, Max Fuerst, Meghan…" — that's accurate. The five seats need to sign in via Microsoft on Monday for their per-member rows to populate.

### Vercel env state

- `MS_TENANT_ID` — set to your real tenant GUID ✓
- `MS_CLIENT_ID` / `MS_CLIENT_SECRET` — set ✓
- `INSTINCT_GRAPH_APP_ONLY` — `false` (intentional; turn back on only after probe + Azure consent verified)
- `INSTINCT_DB_ALLOW_INSECURE` — unset (good; cert verification active)
- All other previously-set vars unchanged.

### Azure state

- Wolfpack Instinct app registration in Azure Entra ID has the existing delegated permissions working.
- Application-permission consent for `Mail.Read`, `Calendars.Read`, `Tasks.Read.All`, `MailboxSettings.Read`, `User.Read.All` was clicked but does NOT appear to be effectively granted (Graph data calls 403'd from the app-only token in production). Either consent didn't propagate OR the permissions were added to the wrong section. Verify Monday before re-enabling app-only.

---

## The principles UI today

After Vercel deploys + a `Run all` click, the Team scoreboard shows real data for both connected members across all 10 principles. Sample:

- Reality Over Optics: Hoxsie 11 / -0.24, Homyk 6 / -0.15
- Async Default: Hoxsie 23 / -0.31, Homyk 12 / -0.28
- Direct Over Delayed: Hoxsie 6 / 0.30, Homyk 6 / 0.47
- Respect off-hours: Hoxsie 8 / -0.60, Homyk 1 / -0.60
- Calendar Hygiene: Hoxsie 16 / -0.07, Homyk 11 / 0.14
- Define Done / Red Early / Pushback: each shows `(team-wide)` 9 / -0.40 (KR validators).

Homyk's My principles tab populates correctly with personal rows. Hoxsie's My principles tab is empty — see TL;DR #1.

---

## First thing to do Monday

1. **Send Hoxsie a one-line note** when he comes online:

   > Hey — when you log into Instinct Monday, **click "Sign in with Microsoft"** on the login page (not the email/password form). Your current session is a leftover demo account; signing in with your `nick@thewolfpack.agency` Microsoft credentials connects your real calendar/mail/tasks data so the principles dashboard shows your actual rows.

2. **After he signs in correctly**, verify his My-principles tab populates. Should show ~11 personal observations on Reality Over Optics, ~23 on Async Default, etc. If it's still empty, get his localStorage `instinct_user` value FIRST before debugging.

3. **Trigger Run all** on `/principles` after Hoxsie signs in so observations include his real connected token.

4. **Check Vercel build status** for any overnight deploy errors:
   ```
   gh run list --repo the-wolfpack-agency/wolfpack-apex --limit 5
   ```

---

## Open follow-ups

### High value, easy wins (≤ 30 min each)

1. **Disable demo seed accounts.** One-line migration:
   ```sql
   UPDATE instinct_team_members SET is_active = FALSE WHERE LOWER(email) LIKE '%@wolfpack.dev';
   ```
   Prevents future "user signed in as demo CEO" confusion. Also strip the demo-credentials block from `/login`.

2. **App-only probe-and-fallback.** Before re-enabling `INSTINCT_GRAPH_APP_ONLY`, add a startup-time probe that hits `/users/{me}/mailFolders/sentitems` with the app-only token. If 401/403 → flag as unavailable for the process and fall back to delegated. ~15 min.

3. **"Connect Microsoft" prompt on My-principles tab when user has no `instinct_ms_tokens` row.** Eliminates the next ambiguity.

### Medium

4. **Verify Azure Application-permission consent.** Portal → App registrations → Wolfpack Instinct → API permissions. Each of `Mail.Read`, `Calendars.Read`, `Tasks.Read.All`, `MailboxSettings.Read`, `User.Read.All` must show **Application** type AND the green "Granted for [tenant]" status. Re-grant if any are missing/yellow. Then flip `INSTINCT_GRAPH_APP_ONLY=true` (after probe lands) and verify with one Run all.

5. **Convert remaining 9 validators to `getReadTokenForUser` orchestrator** so unconnected seats get observations via app-only too. Dependent on #4.

6. **Tighten `tasks.weekly_priority_count` zero-tier scoring.** Currently emits +0.5 for "0 high-importance active tasks" — could indicate either healthy commitment OR no work tracked at all. Distinguish by checking whether ANY tasks were created this window.

### Low

7. **Filter `showAs='oof'` in `meeting_agenda_present`** so OOO holds don't show as "no agenda" drift.

8. **Dependabot triage** — 2 moderate vulns flagged on default branch all session. Probably benign but worth one PR.

9. **Re-enable lint rules suppressed in PR #77** — the two `useMemo` errors in `principles/page.tsx` are pre-existing and need investigation in a focused session.

---

## Files most likely to be touched next session

| Path | Why |
|---|---|
| `src/db/migrations/130_*.sql` | Demo-account disable + any next migration |
| `src/lib/microsoft-graph.ts` | App-only probe-and-fallback |
| `src/lib/principles/evaluators/*.ts` | Convert remaining 9 to `getReadTokenForUser` |
| `src/app/(dashboard)/principles/page.tsx` | "Connect Microsoft" CTA when no tokens |
| `src/app/api/principles/me/route.ts` | If Hoxsie's tab still misbehaves Monday |

---

## Memory invariants reinforced today (CRITICAL for next session)

- **`feedback_get_runtime_data_first.md`** — When a user-visible bug doesn't reproduce in unit tests, REFUSE to ship code until I've seen real prod state (localStorage, API response, env var status, DB row contents). Tests with mocks I wrote prove nothing about prod state mismatch. Default suspicion order: wrong-account login → unset env → migration didn't run → not deployed yet → my code logic.
- **`feedback_verify_with_vercel_build.md`** (existing, reinforced) — Run `npm run vercel-build` (or at minimum `npx tsc --noEmit`) locally before pushing changes that touch type unions, migrations, or auth-cookie shape. Today's `WriteQueryError("empty_parsed_set")` literal-union miss + the `issueRefreshToken` options-object mistake + the missing `setInstinctSession` import would all have been caught locally.
- **`feedback_no_silent_data_loss.md`** (existing, reinforced) — `syncPrinciplesFromParsed` used to silently mass-retire when called with `parsed=[]`. Now throws `WriteQueryError("empty_parsed_set")`. Every destructive code path needs an empty-input guard.

---

## What got committed by mistake today

Nothing on the wrong repo. All apex commits landed on `the-wolfpack-agency/wolfpack-apex`.

But: 8 deploy failures from Vercel that should have been caught locally. The pattern (rush a fix → push → wait for Vercel → fail → patch → repeat) cost the user an entire Saturday hour. Not making this list in the future is the goal of the new memory rule.

---

## One thing to celebrate

The principles platform is qualitatively better than yesterday:

- 8 new validators across 4 surfaces (calendar, mail, tasks, goals) — dropping in a new principle now requires zero code most of the time, just signal lines that match keyword matchers.
- Microsoft sign-in is one click + zero password management for the team.
- The team scoreboard correctly distinguishes per-member rows from `(team-wide)` rows, with names rolling up cleanly even across legacy duplicate ids.
- The SharePoint write-back loop for the weekly principles report is wired and tested — Hoxsie will get a generated `Wolfpack — Last Week vs Operating Principles.docx` Monday morning if the cron fires.
- Data integrity has guards now (UNIQUE constraints, ON CONFLICT DO NOTHING, empty-parse refusal) where there used to be silent failure modes.

The hour spent guessing on Hoxsie's tab was painful. Everything else from today is real progress that makes the team's Monday morning useful.
